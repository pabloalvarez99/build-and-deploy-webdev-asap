Read the following files to understand current project state, then continue with pending work:

1. Read `bitacora.md` if it exists - project log with current tasks
2. Read `HANDOVER.md` if it exists - previous session summary
3. Check `git status` and `git log --oneline -5` for recent changes
4. Check if there are uncommitted changes that need to be pushed

Then identify and execute the next pending task. If no tasks are pending, ask what to work on next.
